services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: kibunmeshi_production
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kibunmeshi_production"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
        GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
    env_file:
      - .env.production
    environment:
      RAILS_ENV: production
      SOLID_QUEUE_IN_PUMA: "true"
      DATABASE_URL: postgres://postgres:password@db:5432/kibunmeshi_production
    ports:
      - "80:3000"
    depends_on:
      db:
        condition: service_healthy

volumes:
  db_data:
