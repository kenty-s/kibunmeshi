<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:page_title) ? yield(:page_title) : "kibunmeshi" %></title>
    <meta name="application-name" content="kibunmeshi">
    <meta name="apple-mobile-web-app-title" content="kibunmeshi">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <% og_title = content_for?(:og_title) ? yield(:og_title) : "kibunmeshi" %>
    <% og_description = content_for?(:og_description) ? yield(:og_description) : "Find a dish based on your mood." %>
    <% og_url = content_for?(:og_url) ? yield(:og_url) : root_url %>

    <% og_image =
      if content_for?(:og_image)
        yield(:og_image)
      else
        image_url("ogp.png")
      end
    %>

    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= og_title %>">
    <meta property="og:description" content="<%= og_description %>">
    <meta property="og:url" content="<%= og_url %>">
    <meta property="og:image" content="<%= og_image %>">
    <meta property="og:image:secure_url" content="<%= og_image %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= og_title %>">
    <meta name="twitter:description" content="<%= og_description %>">
    <meta name="twitter:image" content="<%= og_image %>">
    <meta name="twitter:image:alt" content="kibunmeshi">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application" %>
    <%= favicon_link_tag 'favicon.png' %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'shared/header' %>

    <% if flash.any? %>
      <div class="container mx-auto px-4 mt-4">
        <% flash.each do |type, message| %>
          <% next if message.blank? %>
          <div class="mb-3 rounded-md px-4 py-3 text-sm <%= type.to_s == 'notice' ? 'bg-green-50 text-green-800 border border-green-200' : 'bg-red-50 text-red-800 border border-red-200' %>">
            <%= message %>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="w-full">
      <%= yield %>
    </main>

    <%= render 'shared/footer' %>
  </body>
</html>
